kind: Service
apiVersion: v1
metadata:
  name: flask-service
  namespace: expense
spec:
  selector:
    app: flask
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
